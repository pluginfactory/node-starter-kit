/**
 * @description this is a sample process that could be modified/edited/renamed to
 * as per the background process implementation
 * @author {{app_author}}
 * @since {{app_date}}
 */
export default ({ params }) => {
	/**
	 * @todo handle the background process here.
	 * Like sending notifications,
	 * deleting bulk data
	 * sending OTPs and verification emails etc.
	 */
	console.log('This is a sample process');
};
